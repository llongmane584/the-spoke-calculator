# CLAUDE.md

## Project Overview

スポーク長計算Webアプリケーション。自転車ホイール組みに必要なスポーク長を計算する。


# Issue 駆動の開発フロー

コード変更は次の順で進める。

1. **計画**: 改善方針を提示し、合意を取る
2. **記録**: 合意した実装計画を `gh issue comment` で issue に残す
3. **ブランチ**: 目的が分かる名前で feature ブランチを作成する
4. **実装**
5. **テスト**: 動作を確認する（Python は `.venv` 内で実行）
6. **統合**: commit・push して PR を作成する


## Branching and Releases

- `dev` が統合ブランチ。機能追加も修正も PR は `dev` 宛てに出す。
- `main` は本番そのもの。push した瞬間 GitHub Pages へデプロイされる。
- リリース (version++ / タグ / GitHub Release / アプリ内更新履歴) は
  [`docs/RELEASE.md`](docs/RELEASE.md) の手順で行う。リリース作業では issue もブランチも立てない。


# コードスタイル

- 改行コードは LF。行末に余分な空白を残さない
- 過剰な設計をしない
- 成功したように見せるだけの偽フォールバックは禁止。それは例外の握りつぶしであって
  フォールバックではない。エラーは早期に、明示的に通知する
- コンソールに出るエラー・警告は、重要度を問わずすべて解消する
  - 「本番ビルドでは出ない」「SSR していないので出ない」「実害はない」は理由にならない。
    開発ビルドだけの警告も対象。警告を無視することに慣れると重大なエラーも見落とす
  - すぐ直せないものはコードコメントで正当化せず、issue として起票する
  - 実例 (spoke-length-calculator #120): React の DOM ネスト警告を「実害はない」と
    コードコメントに書いて放置した。実際には消す手があり、調査を打ち切らせるだけの
    有害な記録だった。同じ形の「実害はない」を二度と書かない

# その他

- 一時ディレクトリが必要なら、プロジェクト内に `temp/` または `tmp/` を作って使う。
  プロジェクト外へのアクセスは原則禁止。作った一時ディレクトリは `.gitignore` に追加する
- `gh issue view` は `--repo` を付けずに使う。リポジトリ情報がどうしても必要なときだけ
  `.git/config` を参照する


# その他

- 一時ディレクトリが必要なら、プロジェクト内に `temp/` または `tmp/` を作って使う。
  プロジェクト外へのアクセスは原則禁止。作った一時ディレクトリは `.gitignore` に追加する
- `gh issue view` は `--repo` を付けずに使う。リポジトリ情報がどうしても必要なときだけ
  `.git/config` を参照する

# ツール

- 検索は `grep` ではなく `rg` (ripgrep) を使う
- `playwright-cli`: ブラウザ自動化・スクリーンショット用（npm でグローバル導入済み）
  例: `playwright-cli resize 1440 900 && playwright-cli screenshot --filename=desktop-1440.png 2>&1`

## クソダサUIデザイン禁止

- クソダサいグラデーション禁止
- クソダサい青紫系配色禁止
- GitHub へのテキスト送信は、 ./tmp/ 以下に issue 番号のサブディレクトリに一時ファイルを生成し、 `--body-file` で行うこと
- オープンPRの修正の場合、新規ブランチを作成せず、新規Issueを起票しない
- 実装完了後、ブラウザーのコンソールにエラーも警告も出ていないことを確認する
  (Code Style Guidelines のコンソールの項を参照)
